$(function () {    $(".tester").click(function (e) {        $.ajax({            type: "POST",            url: "index.php?option=com_ajax&plugin=tester&method=tester&format=json",            success: function (data) {                //var response = jQuery.parseJSON(data);                console.log(data);            }        });        e.preventDefault();    });    $('#content-area').keditor();    $('.editor').addClass('wf-editor-toggle-off');    var currentContent = $('#jform_fulltext').val();    if (currentContent != '') {        //console.log(currentContent);        $('#content-area').keditor('setContent', currentContent);        //$('.keditor-content-area').keditor('setContent', '<section><div class="row"><div class="col-md-6" data-type="container-content"><section data-type="component-text">New content</section></div></div></section>');    }    ;    // Reset layout    $('.resetLayout').on('click', function (e) {        e.preventDefault();        $('#content-area').keditor('setContent', '');    });    // Save layout to text editor    $(document).on('contentchange', function (e) {        e.preventDefault();        //e.stopPropagation();        var editorContent = $('#content-area').keditor('getContent');        var editorContentHTML = $('#content-area').html();        var withoutSection = $('#content-area').keditor('getContentWithoutSection');        var contentWithoutSection = $(".hello").remove();        //var saveUrl = postMessageData && postMessageData.pageName ? postMessageData.pageName : fileName;        console.log('Changed layout' + editorContent);        $('#jform_kickpagebuilder').val(editorContent);        $('#jform_withoutsections').val(withoutSection);        $('#codepreview').html(withoutSection);    });    $('.saveLayout').on('click', function (e) {        e.preventDefault();        //e.stopPropagation();        var editorContent = $('#content-area').keditor('getContent');        var withoutSection = $('#content-area').keditor('getContentWithoutSection');        var contentWithoutSection = $(".hello").remove();        //var saveUrl = postMessageData && postMessageData.pageName ? postMessageData.pageName : fileName;        $('#jform_kickpagebuilder').val(editorContent);        console.log('Saved layout' + editorContent);        $('#jform_withoutsections').val(withoutSection);        $('#codepreview').html(withoutSection);        /*        $.ajax({            url: saveUrl,            type: 'POST',            data: {                body: fileContent            },            dataType: 'json',            success: function () {                if (isEmbeddedIframe) {                    doPostMessage({                        isSaved: true,                        resp: postMessageData.resp,                        willClose: postMessageData.willClose                    }, postMessageData.url);                } else {                    Msg.success('File is saved!');                }                hideLoadingIcon();                body.removeClass('content-changed');            },            error: function (e) {                Msg.error(e.status + ': ' + e.statusText);                hideLoadingIcon();            }        })        */    });});